# V5 最終符合性執行計畫

目標：完成 V5 規格最後一項首頁首屏要求，並以既有 V5 與 UX 2.0 功能為基礎發布，不改動官方資料、金融定義或交易日邏輯。

## 已完成

- [x] 盤點 V5 規格與現有實作：真實 OHLCV K 線、日／週／月聚合、公司整合頁、跨市場搜尋、行動版與公開資料投影均已存在。
- [x] 建立純函式首頁 fallback，僅從已驗證的興櫃、IPO、CB 公開 snapshot 產生日期、摘要與事件。
- [x] 將 fallback 接入靜態 staging；所有 generation、manifest、runtime 與資料產物驗證通過後才寫入發布首頁。
- [x] 將無資料的頁面文案統一為中性破折號，移除讀取、placeholder、資料狀態與內部診斷文案。
- [x] 加入首頁 renderer 與 staging 回歸測試，保護公開輸出邊界。
- [x] 執行 lint、型別檢查、全量建置與回歸測試。

## 相容性決定

保留較新的「市場事件」入口作為 V5 四個核心研究入口之外的已核准延伸，不回退既有 UX 2.0 功能。

## 發布門檻

- 已驗證資料才可顯示；無法證實的值為 `—`。
- 不輸出來源識別、缺漏原因、技術診斷或個資。
- 提交前須再次通過 `npm run lint`、`npm run typecheck` 與 `npm test`。
